# ============================================================================
# Redrob AI Hiring Intelligence Platform — Python dependencies
# ============================================================================
# sentence-transformers and torch have been removed because PyTorch does not
# publish cp314 wheels that load correctly on Windows (c10.dll DLL failure).
# The semantic_scorer.py module now uses TF-IDF + cosine similarity from
# scikit-learn, which is already present here and has zero install risk.
#
# To restore sentence-transformer scoring on Python 3.10/3.11/3.12:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install sentence-transformers
# and replace src/redrob_ranker/semantic_scorer.py with the ST version.
# ============================================================================

# Core ML / data
numpy>=1.24.0
scikit-learn>=1.3.0

# Data handling & validation
pandas>=2.0.0
pydantic>=2.0.0
pyyaml>=6.0
python-dotenv>=1.0.0

# Optional: API server
fastapi>=0.100.0
uvicorn>=0.23.0

# Optional: dashboard
streamlit>=1.25.0
plotly>=5.15.0

# Development & testing
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.0.0
isort>=5.12.0
flake8>=6.1.0
